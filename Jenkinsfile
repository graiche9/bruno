pipeline {
  agent any
  stages {
    
    stage('Test API Bruno') {
      steps {
        sh ''
      }
    }
    stage('Install Bruno CLI') {
      steps {
        sh 'npm install -g @brunoc/cli'
      }
  }
}
}